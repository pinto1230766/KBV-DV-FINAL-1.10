<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========================================== -->
    <!-- AUTORISATIONS INTERNET ET RÉSEAU -->
    <!-- ========================================== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- ========================================== -->
    <!-- AUTORISATIONS NOTIFICATIONS LOCALES -->
    <!-- ========================================== -->
    <!-- Notification pour Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Alarmes et notifications planifiées -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- Vibration pour les notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Maintenir l'appareil éveillé pour les notifications -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Redémarrer les notifications après le redémarrage du système -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ========================================== -->
    <!-- AUTORISATIONS CAMÉRA -->
    <!-- ========================================== -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Déclaration des fonctionnalités caméra (non obligatoires) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!-- ========================================== -->
    <!-- AUTORISATIONS STOCKAGE -->
    <!-- ========================================== -->
    <!-- Pour Android 12 et inférieur (API 32 et inférieur) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="32" />

    <!-- Pour Android 13+ (API 33+) - Accès aux médias -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Pour Android 13+ - Gestion des fichiers (nécessaire pour la sauvegarde) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
                     android:minSdkVersion="30"
                     android:maxSdkVersion="32"
                     tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_MEDIA" />

    <!-- ========================================== -->
    <!-- AUTORISATIONS GÉOLOCALISATION -->
    <!-- ========================================== -->
    <!-- Localisation précise (GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    
    <!-- Localisation approximative (réseau) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Localisation en arrière-plan (décommentez si nécessaire) -->
    <!-- ATTENTION : Nécessite une justification pour le Google Play Store -->
    <!-- <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> -->

    <!-- ========================================== -->
    <!-- AUTORISATIONS SYSTÈME -->
    <!-- ========================================== -->
    <!-- Service en premier plan (pour les notifications persistantes) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- Empêcher l'appareil de se mettre en veille -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- ========================================== -->
    <!-- CONFIGURATION DE L'APPLICATION -->
    <!-- ========================================== -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true">

        <!-- Activité principale -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Provider pour partager des fichiers -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Receiver pour les notifications après redémarrage -->
        <receiver
            android:name="com.capacitorjs.plugins.localnotifications.LocalNotificationRestoreReceiver"
            android:exported="true"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
